import { createContext } from "react";

export const Appcontext=createContext()

//USE-CONTEXT =Used to control/Avoid props-drill
//PROPS-DRILL =To transfer value/data from one component to another.
//PROPS =Data that are being transmitted.